public class OperadorTernario {
    public static void main(String[] args) {
        // condiciona tipos if else de forma simplificada
        // tiene 3 secciones
        // Definicion -> se evalua una expresion y ejecuta una parte de codigo u otra
        // var resultado = (expresion) ? true : false
        var resultado = (3>2) ? true : false;
        System.out.println("Resultado: " + resultado);
        // Util utilizar var
        //var resultado = (3>2) ? "verdadero" : false;
    }
}
